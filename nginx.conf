user                nginx;
worker_processes    auto;
pid                 /run/nginx.pid;

events {
    worker_connections  1024;
}

http {
    upstream website {
        server              website:8000;
    }

    server {
        listen              444;
        server_name         ists.ritsec.club;

        location / {
            include             uwsgi_params;
            uwsgi_pass          website;
        }
    }
}
